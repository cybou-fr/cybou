set(ts_files
  # CYBOU ships no translations yet: the inherited Bitcoin Core locale
  # files surface untranslated legacy UI (e.g. the French diagnostics
  # window) while CYBOU strings stay English. Keep only the master
  # template; real CYBOU translations will replace this list.
  bitcoin_en.ts
)
